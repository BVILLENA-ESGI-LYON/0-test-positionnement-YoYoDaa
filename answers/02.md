### Comment une classe implémente une interface ? Est-il possible d'implémenter de plusieurs interfaces, si oui comment ?

voici comment utilisé "implements" :

```php
class Enfant implements class2 {
}
```
